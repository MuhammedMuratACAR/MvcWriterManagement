@model EntityLayer.Concrete.Category

@{
    Layout = "~/Views/Shared/_ModalLayout.cshtml";
    ViewBag.ModalTitle = "Kategori Ekle";
    ViewBag.ControllerName = "AdminCategory";

}


@using (Ajax.BeginForm("AddCategory", "AdminCategory", new AjaxOptions() { HttpMethod = "POST", UpdateTargetId = "frmEmp" }))
{

    @Html.AntiForgeryToken()

    <span class="alert-danger">
        @Html.ValidationSummary()
    </span>
    <div class="form-group">
        @Html.Label("Kategori Adı")
        @Html.TextBoxFor(x => x.CategoryName, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.CategoryName, "", new { @class = "text-danger" })
        <br />
        @Html.Label("Açıklamayı Yazınız")
        @Html.TextAreaFor(x => x.CategoryDescription, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.CategoryDescription, "", new { @class = "text-danger" })

        <br />
        <div class="form-group">
            <button type="submit" class="btn btn-primary rounded text-white font-weight-bold">Kaydet</button>
        </div>
    </div>
}


@*<form class="form-group" method="post">
        <input type="text" placeholder="Kategori Adı" class="form-control" name="CategoryName" />
        <input type="text" placeholder="Açıklama Giriniz" class="form-control" name="CategoryDescription" />
        <button class="btn btn-primary">Kategori Ekle</button>
    </form>*@

